# Core Python dependencies
fastapi==0.95.1
uvicorn[standard]==0.23.2

# File and text processing
PyMuPDF==1.19.7  # for PDF reading (imported as fitz)
pytesseract==0.3.10
Pillow==9.5.0  # for image processing (PIL)
python-multipart==0.0.5  # for file uploads in FastAPI
pandas==2.0.2
pdf2image==1.16.3 # if needed for alternate PDF to image conversion (optional)
opencv-python-headless==4.7.0.72 # optional, in case of advanced image OCR needs

# Vector stores and LLM support
langchain==0.0.224
faiss-cpu==1.7.2  # for vector similarity search
transformers==4.30.0
sentence-transformers==2.2.2
accelerate==0.18.3  # for optimizing HF models
torch==2.0.1  # required for transformers and sentence-transformers
scikit-learn==1.2.2  # used internally by some embedding models

# For file and repo management (optional but safe)
gitpython==3.1.31  # if loading repos directly

# Additional dependencies
pydantic==2.0.3  # for data validation (used in FastAPI)
python-dotenv==1.0.0  # for environment variable management
langchain-openai==0.0.4  # integration with OpenAI models in LangChain
openai==0.27.0  # OpenAI API client
pathlib==1.0.1  # (built-in for Python >= 3.4, not strictly needed)
pickle5==0.0.11  # to ensure compatibility with Python’s pickle (useful if you're working with legacy versions)
hashlib==0.3  # secure hash algorithms for file verification (already part of Python's standard lib)
stat==1.0.0  # file status utilities (standard lib)
shutil==1.0.0  # high-level file operations (standard lib)
mangum

